# 📦 Sürüm & Görev Kaydı: v1.5.8 - Google Sign-In OAuth Client ID Yapılandırması & Akıllı Dinamik Doz/Kutlama Zamanlaması

**Tarih:** 30 Ağustos 2026  
**Saat:** 00:15 (TSİ)  
**Sürüm:** `v1.5.8` (Build `35`)  
**Modül:** Auth / `authService` / `MedicineAddedCelebrationModal` / `useMedicinePersistence` / `.env`  
**Mimarlar & Geliştiriciler:** Anti Agent (Antigravity) & ZCode GLM-5.3-Flash (High Reasoning Mode)  
**Hedef Cihazlar:** Samsung Galaxy Tab S7 FE (`R52TB0HJREP`) & Xiaomi (`43cebdf1`)

---

## 🎯 1. Kullanıcı Bildirimleri & Talepler

1. **Google ile Giriş Hatası:**
   - Yeni Firebase projesinde (`ilacantiv1`) Google sağlayıcısı aktifleştirildikten sonra üretilen OAuth 2.0 Web Client ID (`708668760763-2ta9pf3rrtn8cg7ihf16tsct42e06mq6.apps.googleusercontent.com`) sisteme girildi.
2. **Kutlama Modalı Mantık Hatası:**
   - İlaç güncellendiğinde veya akşam saatlerinde kayıt yapıldığında geçmiş saatleri *"İlk doz bugün saat 08:00'de"* şeklinde göstermesi düzeltildi.

---

## 🔍 2. Kök Neden Analizi & Çözüm Mimarisi (ZCode GLM-5.3 Flash High)

1. **Google OAuth 2.0 Client ID Eşitlemesi:**
   - `mobile/.env`, `mobile/app.json`, `mobile/app.config.json` ve `mobile/src/services/authService.ts` dosyalarına yeni Google Web Client ID tanımlandı.
2. **Akıllı Dinamik Doz Tespiti:**
   - İlacın tüm hatırlatma saatleri anlık saatle (`HH:mm`) karşılaştırılır.
   - Bugün henüz vakti gelmemiş bir doz varsa:
     - Yeni ilaç ise: `İlk doz bugün saat SS:DD'de ⏰`
     - Güncelleme ise: `Sonraki doz bugün saat SS:DD'de ⏰`
   - Bugünün tüm doz saatleri geçmişse:
     - Yeni ilaç ise: `İlk doz yarın saat SS:DD'de ⏰`
     - Güncelleme ise: `Sonraki doz yarın saat SS:DD'de ⏰`
   - Refakatçi WhatsApp paylaşım metinleri de bu mantığa dinamik olarak bağlandı.

---

## 🛠️ 3. Yapılan Teknik Değişiklikler

### 1. `mobile/.env`, `mobile/app.json`, `mobile/app.config.json`, `mobile/src/services/authService.ts`
- `GOOGLE_WEB_CLIENT_ID` ve `GOOGLE_ANDROID_CLIENT_ID` güncellendi (`708668760763-2ta9pf3rrtn8cg7ihf16tsct42e06mq6.apps.googleusercontent.com`).

### 2. `mobile/src/hooks/useMedicinePersistence.ts` & `MedicineAddedCelebrationModal.tsx`
- `isTomorrow` bayrağı ve dinamik `nextReminder` algoritması eklendi.
- "İlk doz" / "Sonraki doz" ayrımı `isEditing` durumuna bağlandı.

### 3. Birim Testleri
- `src/__tests__/components/addMedicine/MedicineAddedCelebrationModal.test.tsx` koşuldu (`5 passed, 5 total`).

### 4. Sürüm ve Konfigürasyon
- `mobile/src/config/version.ts` -> `APP_VERSION = '1.5.8'`, `ANDROID_VERSION_CODE = 35`, `IOS_BUILD_NUMBER = '35'`.
- `mobile/package.json` -> `1.5.8`.
- `mobile/app.json` -> `1.5.8` (Build `35`).

---

## 📱 4. Derleme, Paketleme ve Cihaz Testleri

1. **JS Bundle:** `npx react-native bundle` -> Başarılı (23 asset kopyalandı).
2. **Gradle Release APK:** `.\gradlew assembleRelease --no-daemon` -> `BUILD SUCCESSFUL in 4m 31s`.
3. **APK Kayıtları:**
   - `IlacHatirlatici_v1.5.8_release.apk`
   - `app-release.apk`
   - `apk/IlacHatirlatici_v1.5.8_release.apk`
4. **Cihaz Yüklemeleri:**
   - Samsung Galaxy Tab S7 FE (`R52TB0HJREP`): `Performing Streamed Install -> Success`

---

## 👥 5. Ajan Eylemleri & Katkıları

- **ZCode (GLM-5.3 Flash High):** Google OAuth 2.0 mimari analizi, dinamik hatırlatma saatleri algoritması, kullanıcı deneyimi iyileştirmesi.
- **Anti Agent (Antigravity):** Kodlama, .env ve app.json yapılandırmaları, Jest testleri, JS bundle ve Gradle APK derlemesi, Samsung cihaz kurulumu ve zorunlu arşiv protokolünün tamamlanması.
