# 📦 Sürüm & Görev Kaydı: v1.5.7 - Yeni Firebase Projesine Geçiş (`ilacantiv1`), Konfigürasyon & Canlı Dağıtım

**Tarih:** 29 Ağustos 2026  
**Saat:** 22:15 (TSİ)  
**Sürüm:** `v1.5.7` (Build `34`)  
**Modül:** Firebase Backend / `google-services.json` / `firestore.rules` / `.firebaserc` / `app.json`  
**Mimarlar & Geliştiriciler:** Anti Agent (Antigravity) & ZCode GLM-5.3-Flash (High Reasoning Mode)  
**Hedef Cihazlar:** Samsung Galaxy Tab S7 FE (`R52TB0HJREP`) & Xiaomi (`43cebdf1`)

---

## 🎯 1. Kullanıcı Talebi & Hedef

> **Kullanıcı:** *"firebase yeni bir proje klasörüne taşıyabilir miyiz"*

Kullanıcının Firebase Console üzerinde açtığı yeni ve temiz **`ilacantiv1`** projesine uygulamanın tüm Android, Web, Firestore ve Firebase CLI yapılandırmaları eksiksiz şekilde aktarılmıştır.

---

## 🔍 2. Kökten Uca Geçiş Analizi (ZCode GLM-5.3 Flash High)

1. **Yeni Firebase Proje Kimlikleri:**
   - **Project ID:** `ilacantiv1`
   - **Project Number:** `708668760763`
   - **App ID (Android):** `1:708668760763:android:2fb620035e210d5ca3ff9e`
   - **API Key:** `AIzaSyAsqXQZZiVM1EPF0k8MW_b2AsiUv4XSJhM`
   - **Storage Bucket:** `ilacantiv1.firebasestorage.app`
   - **Auth Domain:** `ilacantiv1.firebaseapp.com`

2. **Uygulama İçi & Dışı Tüm Konfigürasyonların Eşitlenmesi:**
   - İndirilen güncel `google-services.json` Android modülüne işlendi.
   - `.firebaserc`, `mobile/app.json`, `mobile/app.config.json` ve yardımcı scriptler yeni projeye bağlandı.
   - `firestore.rules` güvenlik kuralları Firebase CLI ile yeni projeye derlenip canlıya alındı.

---

## 🛠️ 3. Yapılan Teknik Değişiklikler

### 1. `google-services.json`
- `mobile/google-services.json` ve `mobile/android/app/google-services.json` dosyaları yeni `ilacantiv1` yapılandırmasıyla değiştirildi.

### 2. Yapılandırma Dosyaları
- `.firebaserc` -> `"default": "ilacantiv1"` olarak güncellendi.
- `mobile/app.json` & `mobile/app.config.json` -> Yeni API Key, AuthDomain, ProjectId ve StorageBucket tanımlandı.
- Yardımcı scriptler (`importTITCK.js`, `seedMedicines.js`, `setupAIConfig.js`, `inspect_firestore.js`, `diagnose.js`) yeni projeye bağlandı.

### 3. Firestore Güvenlik Kuralları Dağıtımı
- `npx firebase-tools deploy --only firestore:rules` -> `ilacantiv1` veritabanına başarıyla deploy edildi.

### 4. Sürüm ve Konfigürasyon
- `mobile/src/config/version.ts` -> `APP_VERSION = '1.5.7'`, `ANDROID_VERSION_CODE = 34`, `IOS_BUILD_NUMBER = '34'`.
- `mobile/package.json` -> `1.5.7`.
- `mobile/app.json` -> `1.5.7` (Build `34`).

---

## 📱 4. Derleme, Paketleme ve Cihaz Testleri

1. **JS Bundle:** `npx react-native bundle` -> Başarılı (23 asset kopyalandı).
2. **Gradle Release APK:** `.\gradlew assembleRelease --no-daemon` -> `BUILD SUCCESSFUL in 5m 2s`.
3. **APK Kayıtları:**
   - `IlacHatirlatici_v1.5.7_release.apk`
   - `app-release.apk`
   - `apk/IlacHatirlatici_v1.5.7_release.apk`
4. **Cihaz Yüklemeleri:**
   - Samsung Galaxy Tab S7 FE (`R52TB0HJREP`): `Success`
   - Xiaomi (`43cebdf1`): `Success`

---

## 👥 5. Ajan Eylemleri & Katkıları

- **ZCode (GLM-5.3 Flash High):** Firebase geçiş mimarisi, proje bağımlılıklarının tespiti, Firestore güvenlik modeli analizi.
- **Anti Agent (Antigravity):** Dosya kopyalama (`google-services.json`), konfigürasyon güncellemeleri, Firestore rules deploy işlemi, JS bundle ve Gradle derlemesi, fiziksel cihazlara kurulum ve arşiv protokolünün tamamlanması.
